Use the `npm run check` command to validate correctness and autoamtically format files.
Indentation is also automatically updated by that command.

In any module, prefer exported symbols towards the top and module-internal functions towards the bottom.

When modifying festival ingestion scripts or utilities in `scripts/`, update `.claude/skills/add-festival/SKILL.md` to reflect any pattern changes.

Use the `npm run check` command to validate correctness and autoamtically format files.

The `docs/PROG_SCHEMA.md` to understand the schema of `assets/programmation.json` instead of parsing it.

## File organization

In component files (`.tsx`): props type and component(s) first (after imports), utility/helper functions at the bottom.

In other modules (`.ts`): exported symbols towards the top, module-internal functions towards the bottom.
